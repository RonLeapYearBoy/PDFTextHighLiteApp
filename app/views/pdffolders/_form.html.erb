<%= form_for(@pdffolder) do |f| %>
  <% if @pdffolder.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pdffolder.errors.count, "error") %> prohibited this pdffolder from being saved:</h2>

      <ul>
      <% @pdffolder.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
