<%= form_for(@pdffile) do |f| %>
  <% if @pdffile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pdffile.errors.count, "error") %> prohibited this pdffile from being saved:</h2>

      <ul>
      <% @pdffile.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :pdffolder_id %><br>
    <%= f.number_field :pdffolder_id %>
  </div>
  <div class="field">
    <%= f.label :pdf %><br>
    <%= f.text_field :pdf %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
